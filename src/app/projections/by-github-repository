fromStream('github-events')
.whenAny(function (state, ev) {
    var url = ev.data.url;
    url = url.split("/commits")[0].split('/');
    var repoUser = [];
    repoUser.push(url.pop());
    repoUser.push(url.pop());
    {
        linkTo('repo-' + repoUser[1] + '-' + repoUser[0], ev);
    }
});